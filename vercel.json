{
  "installCommand": "echo skip",
  "buildCommand": "cd wms-frontend && npm install && npm run build",
  "outputDirectory": "wms-frontend/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
