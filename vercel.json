{
  "installCommand": "uv python install 3.12 && uv pip install -r requirements.txt --system --python 3.12",
  "buildCommand": "cd wms-frontend && npm install && npm run build",
  "framework": null
}
